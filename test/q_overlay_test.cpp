#include <gtest/gtest.h>
#include <layer.h>

TEST(QOverlayTest, SanityCheck) {
	EXPECT_EQ(1 + 1, 2);
}

TEST(QOverlayTest, AnotherCheck) {
	EXPECT_TRUE(true);
}

TEST(QOverlayTest, link) {
	EXPECT_EQ(aboba(1), 1);
}